# NextTrack API - Requirements
# Privacy-focused music recommendation API
# CM3035 Advanced Web Design Final Project

# ============== Web Framework ==============
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# ============== Data Validation ==============
pydantic>=2.5.0

# ============== HTTP Client (async) ==============
httpx>=0.25.0

# ============== Environment Configuration ==============
python-dotenv>=1.0.0

# ============== Testing ==============
pytest>=7.4.0
pytest-asyncio>=0.21.0

# ============== Caching (optional but recommended) ==============
redis>=5.0.0

# ============== Notes ==============
# No additional dependencies needed for MusicBrainz/Wikidata/Genius clients
# as they use httpx for HTTP requests and standard library for SPARQL
# Redis is optional - the app falls back to in-memory cache if unavailable
